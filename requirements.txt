django
djangorestframework
djangorestframework-simplejwt
django-cors-headers
psycopg2-binary
cryptography
requests
celery
django-celery-beat
redis
channels
channels-redis
daphne
python-dotenv
# Not on PyPI — pip cannot resolve it, which breaks `pip install -r requirements.txt`
# everywhere (Docker build included). Only api/index.py needs it, for Vercel.
# vercel-wsgi