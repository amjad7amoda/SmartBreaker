{
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": "api",
  "functions": {
    "api/index.py": {
      "maxDuration": 10,
      "memory": 512
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
